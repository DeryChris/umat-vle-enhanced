define([], function() {
    "use strict";
    var t = function(t) {
        this.container = t;
        this.tabs = t.querySelectorAll(".umat-glass-tab");
        if (!this.tabs.length) return;
        this._bindEvents();
    };
    t.prototype.refresh = function() {};
    t.prototype._bindEvents = function() {
        var t = this;
        Array.prototype.forEach.call(this.tabs, function(t) {
            t.addEventListener("pointerdown", function() {
                t.classList.add("is-pressing");
            });
            t.addEventListener("pointerup", function() {
                t.classList.remove("is-pressing");
            });
            t.addEventListener("pointercancel", function() {
                t.classList.remove("is-pressing");
            });
            t.addEventListener("pointerleave", function() {
                t.classList.remove("is-pressing");
            });
            t.addEventListener("click", function() {
                if (window.CustomEvent) {
                    try {
                        t.dispatchEvent(new CustomEvent("umat-glass-tab-changed", {
                            detail: {
                                tab: t
                            }
                        }));
                    } catch (t) {}
                }
            });
            if (window.MutationObserver) {
                new MutationObserver(function() {}).observe(t, {
                    attributes: true,
                    attributeFilter: [ "class" ]
                });
            }
        });
    };
    return {
        init: function() {
            var e = document.querySelectorAll(".umat-glass-tabs");
            Array.prototype.forEach.call(e, function(e) {
                new t(e);
            });
        }
    };
});
//# sourceMappingURL=glassmorph_nav.min.js.map